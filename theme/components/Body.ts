// @flow

import variable from '../variables/platform'

export default (_ /*: * */ = variable) => {
  const bodyTheme = {
    flex: 1,
    alignItems: 'center',
    alignSelf: 'center'
  }

  return bodyTheme
}
